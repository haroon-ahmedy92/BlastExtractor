"""Top-level application package for BlastExtractor.

This package contains the crawler, adapter, database, and API modules that
work together during a crawl run. Importing :mod:`app` itself has no runtime
side effects; the work happens in subpackages such as ``app.crawler`` and
``app.sites``.
"""
